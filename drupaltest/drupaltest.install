<?php

/**
 * Enable feature that contains view on drupaltest module install
 */
 
$features = array('drupal_test_feature');
$enable_dependencies = TRUE;
module_enable($features, $enable_dependencies);
